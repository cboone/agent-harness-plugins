# Checklist

Single-page rapid-reference for the pin-everything skill. One row per surface: what to look for, the pinning command or transform, and the one-line verification.

## Surface-by-Surface

| Surface                                      | Detection                                                                       | Pinning command / transform                                                                                                                                                                                                                          | Verify                                                                                         | Reference                |
| -------------------------------------------- | ------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ------------------------ |
| Action `uses:` ref                           | `grep -RE 'uses:[[:space:]]+[^./][^@]+@' .github/`                              | `gh api repos/<r>/commits/<tag> --jq .sha`; replace tag with SHA, append `# vX.Y.Z`                                                                                                                                                                  | `actionlint`; SHA is 40 hex chars                                                              | `./github-actions.md`    |
| Reusable workflow ref                        | Same as above; line contains `.github/workflows/`                               | Same SHA-pinning treatment                                                                                                                                                                                                                           | Workflow still resolves                                                                        | `./github-actions.md`    |
| Channel-style ref (e.g. `@stable`)           | Action ref with non-numeric tag                                                 | `gh api repos/<r>/commits/<branch> --jq .sha`; comment is `# stable` (or branch name)                                                                                                                                                                | Toolchain resolves at runtime                                                                  | `./github-actions.md`    |
| Inline `node-version: "X"`                   | `grep -RE '^[[:space:]]+node-version:' .github/workflows/`                      | Use `.tool-versions` only if it has a `nodejs` line; else `.nvmrc`; else `.node-version`; else add a `nodejs` line to existing `.tool-versions` or create one                                                                                        | `awk '/^nodejs /' .tool-versions` (or `cat .nvmrc`)                                            | `./language-runtimes.md` |
| Inline `ruby-version: "X"`                   | Same pattern, `ruby-version`                                                    | Use `.tool-versions` only if it has a `ruby` line; else `.ruby-version`; else `Gemfile` (with `ruby` directive); else add a `ruby` line to existing `.tool-versions` or create one                                                                   | `awk '/^ruby /' .tool-versions` (or `cat .ruby-version`)                                       | `./language-runtimes.md` |
| Inline `go-version:`                         | Same pattern, `go-version`                                                      | Replace with `go-version-file: "go.mod"`                                                                                                                                                                                                             | `go.mod` `go X.Y` directive present                                                            | `./language-runtimes.md` |
| Inline `python-version:`                     | Same pattern, `python-version`                                                  | `astral-sh/setup-uv`: omit input (reads `pyproject.toml` `requires-python`); ensure `requires-python` is set; do NOT create `.python-version`. `actions/setup-python`: `python-version-file: ".python-version"`; create `.python-version` if missing | `setup-uv`: `grep '^requires-python' pyproject.toml`; `setup-python`: `cat .python-version`    | `./language-runtimes.md` |
| Inline `zig-version:`                        | Same pattern, `zig-version`                                                     | Action-direct (`mlugg/setup-zig`): omit version input (reads `build.zig.zon` by default). Wrapper (`cboone/gh-actions/.../zig-ci.yml` v2.2.0+): `zig-version-file: build.zig.zon`                                                                    | `build.zig.zon` `minimum_zig_version` present                                                  | `./language-runtimes.md` |
| `packageManager: yarn@X.Y.Z` or `pnpm@X.Y.Z` | `jq -r .packageManager package.json`                                            | `corepack use yarn@X.Y.Z` (or `corepack use pnpm@X.Y.Z`) writes the suffixed `<mgr>@X.Y.Z+sha512.<hash>` into `package.json`; npm has no `packageManager` integrity surface                                                                          | `corepack enable && yarn --version` (or `pnpm --version`)                                      | `./yarn-corepack.md`     |
| `package.json` carets/tildes                 | `jq '.dependencies, .devDependencies' package.json` and grep for `^`/`~`        | App context only: replace with exact lockfile version. Library: leave ranges, pin lockfile only                                                                                                                                                      | Per package manager: `yarn install --immutable`, `pnpm install --frozen-lockfile`, or `npm ci` | `SKILL.md` step 6        |
| `Gemfile` ranges                             | App context: read `Gemfile.lock` for resolved version                           | App: replace `gem 'X', '~> Y'` with `gem 'X', 'Y.Z.W'`. Library (gemspec present): leave ranges                                                                                                                                                      | `bundle install --frozen`                                                                      | `SKILL.md` step 6        |
| `Cargo.toml` carets                          | App context: bin crate with `Cargo.lock` committed                              | App: replace `^X.Y.Z` with `=X.Y.Z`. Library: leave ranges                                                                                                                                                                                           | `cargo build --locked`                                                                         | `SKILL.md` step 6        |
| `pyproject.toml` ranges                      | App context: `requirements.txt` or `uv.lock` committed; no library distribution | App: replace specifiers with `==X.Y.Z`. Library: leave specifiers                                                                                                                                                                                    | `uv pip install --no-deps -e .`                                                                | `SKILL.md` step 6        |
| `go install ...@latest`                      | `grep -RE 'go install [^ ]+@latest'`                                            | Look up upstream latest release; rewrite as `@vX.Y.Z`                                                                                                                                                                                                | `go install` succeeds                                                                          | `./install-commands.md`  |
| `cargo install <crate>`                      | `grep -RE 'cargo install [a-z]'` without `--version`                            | `cargo install --locked --version X.Y.Z <crate>`                                                                                                                                                                                                     | `<crate> --version`                                                                            | `./install-commands.md`  |
| `pip install <pkg>`                          | `grep` for `pip install`, `uv pip install`, or `uv add` without `==`            | `'<pkg>==X.Y.Z'`                                                                                                                                                                                                                                     | `pip show <pkg>` or `uv pip show <pkg>`                                                        | `./install-commands.md`  |
| `uv tool install <pkg>`                      | `grep -RE 'uv tool install [a-zA-Z0-9_.-]+'` without trailing `==`              | `uv tool install '<pkg>==X.Y.Z'`                                                                                                                                                                                                                     | `uv tool list` shows `<pkg> v<X.Y.Z>`                                                          | `./install-commands.md`  |
| `uvx <pkg> [args]`                           | `grep -RE 'uvx [a-zA-Z0-9_.-]+'` without `==` or `@X.Y.Z`                       | `uvx '<pkg>==X.Y.Z' [args]`                                                                                                                                                                                                                          | `uvx '<pkg>==X.Y.Z' --version` (or tool's equivalent version flag)                             | `./install-commands.md`  |
| `npx <tool>`                                 | `grep -RE 'npx [^@ ]+'` without trailing `@version`                             | `npx <tool>@X.Y.Z` in CI without prior install. Skip if local lockfile resolves it                                                                                                                                                                   | `npx <tool>@X.Y.Z --version` (or tool's equivalent version flag)                               | `./install-commands.md`  |
| `.yarnrc.yml`                                | File exists at repo root                                                        | Add `enableScripts: false`, `enableTelemetry: false`, `defaultSemverRangePrefix: ""`. Revert any `approvedGitRepositories: ["**"]`                                                                                                                   | `cat .yarnrc.yml`                                                                              | `./yarn-corepack.md`     |
| `.github/dependabot.yml`                     | File presence                                                                   | Create or merge with weekly schedule, split groups, 10-PR cap, `versioning-strategy: increase` for `npm`                                                                                                                                             | Workflow tab shows Dependabot scheduled                                                        | `./dependabot.md`        |
| `bin/version-audit`                          | Drift coverage for the four surfaces Dependabot can't track                     | Tailor `./scripts/version-audit-template`; write to `bin/version-audit`; pair with workflow                                                                                                                                                          | `./bin/version-audit` exits 0 with no output                                                   | `./version-audit.md`     |

## User-Facing vs Tool-Install Discriminator

Skip pinning when the install path matches any of these patterns — it's a placeholder for the downstream user's project, not a real install:

- `OWNER/REPO`
- `GITHUB-USERNAME/PROJECT-NAME`
- `<owner>/<repo>` or any `<...>` substitution
- Any path under a scaffolded README's `## Usage` or `## Installation` section

## Library vs Application Discriminator

Application context — pin the manifest:

- Node.js: lockfile committed AND (`"private": true` OR no library-style export config)
- Ruby: no `*.gemspec`; `Gemfile.lock` committed
- Python: `requirements.txt` or `uv.lock` committed; no published distribution
- Rust: `Cargo.lock` committed AND has a binary target (any of: `[[bin]]` in `Cargo.toml`, `src/main.rs` or `src/bin/*.rs`, or `cargo metadata` reports a `bin` kind). `publish = false` is also a strong app signal.

Library context — pin the lockfile only, leave the manifest with caret / pessimistic ranges:

- Node.js: package configured for distribution (`name`, `exports`, public)
- Ruby: `*.gemspec` present
- Python: `pyproject.toml` declares a published distribution name
- Rust: crate exposes `[lib]`; `Cargo.lock` typically not committed

When ambiguous, prompt the user.

# main.rs Template (Without Clap)

Create `src/main.rs` with the following content.

```rust
fn main() {
    println!("Hello, world!");
}
```

## Notes

- This is the minimal starting point for a Rust CLI.
- Add argument parsing, subcommands, and business logic as the project grows.
